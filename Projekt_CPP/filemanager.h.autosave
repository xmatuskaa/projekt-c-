#ifndef FILEMANAGER_H
#define FILEMANAGER_H
#include <iostream>

class FileManager
{
private:
    std::string m_file;
public:
    FileManager();
    
    bool loadGame();
    b
    void save();
    void readFromFile(std::string file);

};

#endif // FILEMANAGER_H
